map:{
    ; additionalUserMap = map:{
        ; system = map:{
            ; url = jdbc:mysql://mysqlhost:3306/
            #; schema = ...
            ; user = root
            ; password = df:dfprop/system-password.txt|pass
        }
    }
}
